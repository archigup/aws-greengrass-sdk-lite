[package]
name = "ggl-sdk"
version = "0.0.2"
edition = "2024"
license = "Apache-2.0"
repository = "https://github.com/aws/aws-greengrass-sdk-lite"

[profile.release]
strip = true
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
